/** OpponentReadingPanel — placeholder. Owned by Worker D; reads only from @analysis. */
export default function OpponentReadingPanel() {
  return <div className="overlay-panel">OpponentReadingPanel</div>;
}
